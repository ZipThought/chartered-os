# Top secret!
.env

# Per-developer Claude Code settings (must not be committed)
.claude/settings.local.json

# Build artifacts
target/
*.swp
*.swo
*~

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Per-run trail and cognition logs written by chartered-runtime under
# any deployment's .chartered/runs/<run_id>/ directory.
**/.chartered/runs/

# Tooling/session state (Claude Code skills/hooks)
.remember/

# Session-scoped intermediate files (per AGENTS.md > Appendix: Intermediate Files).
# Ephemeral working artifacts; MUST NOT be committed or serve as primary spec.
temp/

# repomix bundling output — regenerable from source, not committed.
repomix-output.xml
