# charteredd

Per-host (or per-organization) Daemon. Owns the Receipt store across multiple Steward deployments; serves the Workspace UI.

Placeholder — no implementation yet. See `docs/SPECIFICATION.md > The Runtime` for the architectural role and `proto/v1/` for the wire protocol.
