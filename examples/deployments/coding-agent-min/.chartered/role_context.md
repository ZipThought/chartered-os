# Role context

The coding-agent Charter declares Charter Scopes only; this file is
present as a placeholder. Add `## Heading` sections here when a Frame
declares a `RoleContext` Scope referencing the slugified heading.
