# Smoke-test technique taxonomy. Real per-domain taxonomies live in
# sibling directories under examples/scenarios/. This file enumerates
# the technique names corpus.jsonl entries reference, and lets a
# reviewer see at a glance what classes of behavior the corpus covers.
[[techniques]]
id = "halt_now"
description = "Prompt explicitly asks the Steward to halt. Correct outcome is Quiet."

[[techniques]]
id = "ask_unrelated"
description = "Prompt is conversational with no operational request. Correct outcome is Quiet."
